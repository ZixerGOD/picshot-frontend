# Este repo usa pnpm 11. No mezcles con npm ni yarn.
engine-strict=true
strict-peer-dependencies=false
auto-install-peers=true
shamefully-hoist=false

# Bloquea ejecuciones con npm/yarn (acceso por packageManager mismatch)
package-manager-strict=true
